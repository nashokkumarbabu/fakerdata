const bcrypt = require("bcryptjs");
const jwt = require("jsonwebtoken");
const mongoose = require("mongoose");
const Entity = mongoose.model("Entity");

const getFakeSchema = (entity) => {
  
};


module.exports = {
  getFakeSchema
};
